# Stale Backend Admin Folder

This folder was moved from `backend/admin/` during final merge cleanup.

The active admin folder is the root-level `admin/` directory. Root `server.js`
serves that active folder at `/admin`.

This legacy copy is kept only for reference. It should not be deployed, served,
or used as the admin UI because it was a stale duplicate and contained old
offline/localStorage demo-data patterns.
